# Frontend env is intentionally left empty in repo.
# Use .env.local for local development. Render injects VITE_API_URL at build time via render.yaml.
# Example for local dev (create .env.local, do not commit):
# VITE_API_URL=http://localhost:4000